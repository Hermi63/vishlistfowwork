<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 260 160" width="260" height="160">
  <!-- Grey T (upper-left) -->
  <!-- horizontal bar -->
  <rect x="8"  y="10" width="150" height="38" rx="4" fill="#AAAAAA"/>
  <!-- vertical bar -->
  <rect x="52" y="10" width="38"  height="120" rx="4" fill="#AAAAAA"/>

  <!-- Red T (lower-right) — overlaps grey vertical bar -->
  <!-- horizontal bar -->
  <rect x="72" y="62" width="150" height="38" rx="4" fill="#CC1111"/>
  <!-- vertical bar -->
  <rect x="170" y="62" width="38"  height="68"  rx="4" fill="#CC1111"/>

  <!-- Text -->
  <text x="8" y="152"
        font-family="'Arial',sans-serif"
        font-size="19"
        font-style="italic"
        font-weight="700"
        fill="#CC1111">Транспортные</text>
  <text x="145" y="152"
        font-family="'Arial',sans-serif"
        font-size="19"
        font-style="italic"
        font-weight="400"
        fill="#AAAAAA"> Технологии</text>
</svg>
